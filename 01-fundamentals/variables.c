#include <stdio.h>

int main(void)
{
    int age = 20;
    float gpa = 3.75f;
    char grade = 'A';

    printf("Age: %d\n", age);
    printf("GPA: %.2f\n", gpa);
    printf("Grade: %c\n", grade);

    return 0;
}